# Tradebaas Frontend Configuration
# Copy this file to .env.production for production builds

# Backend API URL Configuration
# ============================================================================
# LEAVE EMPTY for same-origin (when using reverse proxy like Caddy/Nginx)
# Example same-origin: Frontend at https://app.example.com, Backend at https://app.example.com/api
#
# SET VALUE for different server deployments
# Example: VITE_BACKEND_URL=http://192.168.1.100:3000
#
# Development: Uses http://localhost:3000 automatically (see src/lib/backend-url.ts)
# ============================================================================
VITE_BACKEND_URL=

# Optional: Override specific environment detection
# VITE_NODE_ENV=production
